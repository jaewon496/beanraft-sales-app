# 빈크래프트 릴스 워커 설정 — 복사해서 .env 로 저장(비공개, git에 올리지 말 것)
# (아래 값은 worker.js 에 기본값으로도 들어가 있어, .env 안 만들어도 동작함)

BC_APP_URL=https://beancraft-sales.netlify.app/
BC_ID=admin
BC_PW=qlwlsltm1
BC_HEADLESS=true          # 첫 검증은 false(창 보임), 상시구동은 true
BC_PROFILE=./.bc-profile  # 로그인 세션 저장 폴더(한 번 로그인하면 유지)
